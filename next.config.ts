import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This dashboard is a full-height two-pane layout; the floating dev badge
  // sits on top of the sidebar footer.
  devIndicators: false,
};

export default nextConfig;
